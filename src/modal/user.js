class UserData {
    constructor (id, userName, photo, ownerId) {
        this.id = id;
        this.userName = userName;
        this.photo = photo;
        this.ownerId = ownerId
    }
}


export default UserData